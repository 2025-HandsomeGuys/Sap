# 데모 출시를 위한 보충 목록 (2026-03-21 기준)

## 기획 전제
- 스태미나 = HP (Pikmin 방식). 0이 되면 게임오버 → 다음날로 전환.
- 게임오버 패널티: 가방 속 미네랄 소실. 골드·업그레이드 상태 유지.
- 다음날이 되면 파놓은 땅 초기화.
- 데모 범위: 1지층(흙/돌) + 2지층(얼음). 2지층 끝에서 다음 지형 떡밥 후 데모 종료.
- 몬스터 없음. 함정/환경 위험으로 긴장감 제공.
- 1지층 → 2지층: 연속된 하나의 맵. 특정 깊이 도달 시 자동 전환.

---

## [필수] 없으면 데모 불가

### 1. 게임오버 시스템
- **현황**: 스태미나 0이 돼도 아무 처리 없음
- **필요한 것**:
  - PlayerStat에서 CurrentStamina == 0 감지 → 게임오버 트리거
  - 쓰러지는 애니메이션 + 가방 미네랄 터져나오는 VFX/이펙트
  - 페이드아웃 연출 후 "N일차에 쓰러졌다" 메시지
  - 미네랄 인벤토리 초기화 (골드·업그레이드는 유지)
  - 자동 저장 후 지상 씬으로 전환

### 2. 다음날 시작 시스템
- **현황**: ExploreExitController → SettlementManager.StopTracking() → 씬 전환까지만 됨. 날짜 카운트·일일 초기화 없음.
- **필요한 것**:
  - 날짜 카운터 (게임오버/자발 복귀 시 +1)
  - 지상 씬 진입 시 "N일차" 표시 연출
  - 다음 탐험 시작 시 월드 초기화 (파놓은 땅 리셋)
  - 자발 복귀(ExploreExitController)와 게임오버 복귀 두 경로 모두 처리

### 3. 현재 깊이 표시 HUD
- **현황**: 없음
- **필요한 것**:
  - 플레이어 y좌표 기반 "깊이 Xm" 실시간 표시
  - 1지층/2지층 구분 텍스트 (지층 경계 통과 시 알림 포함)
  - 참고: GridCoordinateSystem에서 좌표 추출 가능

### 4. 낙하 데미지
- **현황**: StatType에 `FallDamageReduce` 정의만 있고 로직 없음
- **필요한 것**:
  - PlayerController에서 착지 직전 하강 속도 감지
  - 임계 속도 초과 시 스태미나 차감 (낮은 낙하는 무시)
  - FallDamageReduce stat 연동
  - 낙하 피격 피드백 (HitFlashUI 활용)

### 5. 2지층 얼음 메카닉 (동상)
- **현황**: StaminaManager에 `frostbite` 필드 정의만 있고 발동 조건 없음
- **필요한 것**:
  - 2지층 진입 후 시간 경과 또는 특정 구역(OxidizedZone 방식)에서 동상 수치 누적
  - 동상 → MaxStamina 감소 (StaminaManager.AddFrostbite() 이미 구현됨)
  - 동상 시각 피드백 (화면 가장자리 파란 틴트 등)
  - 회복 수단: 소모 아이템 또는 지상 복귀 시 초기화

### 6. 소모 아이템 시스템
- **현황**: StatType에 `ConsumableSlotUnlock` 정의만 있고 구현 없음
- **필요한 것**:
  - 소모 아이템 데이터 (SO 정의 — 스태미나 회복, 동상 해제 등)
  - 소모 아이템 인벤토리 슬롯 UI
  - 사용 입력 및 효과 적용
  - 획득 경로: 상점 구매 또는 드롭

### 7. 데모 엔딩 시퀀스
- **현황**: 없음
- **필요한 것**:
  - 2지층 특정 깊이/지점 도달 시 트리거
  - 다음 지형 떡밥 연출 (간단한 컷씬 or 풀스크린 이미지 + 텍스트)
  - "데모 종료" 안내 후 타이틀로 복귀

---

## [중요] 데모 퀄리티에 직결

### 8. 튜토리얼 / 온보딩
- **현황**: IntroChanger(씬 전환)만 존재. 조작 설명 없음.
- **필요한 것**:
  - 첫 플레이 시 단계별 조작 안내 (이동 → 채굴 → 도구 전환 → 복귀)
  - 스태미나가 HP 역할임을 명시적으로 인지시키는 장치
  - 1회 표시 후 재표시 안 함 (저장 필요)

### 9. 지층 전환 연출
- **현황**: 특정 깊이에 도달해도 아무 피드백 없음
- **필요한 것**:
  - 2지층 진입 시 화면 알림 ("2지층 - 빙하 지대")
  - 배경·조명 변화 (GlobalLightingManager 활용)
  - 동상 메카닉 시작 알림

### 10. 피격 무적 시간 (I-frame)
- **현황**: DamageZone이 인터벌마다 연속 데미지. 함정에 걸리면 순식간에 스태미나 소진 가능성.
- **필요한 것**:
  - 피격 후 짧은 무적 시간 (0.5~1초)
  - HitFlashUI와 연동하여 피격 인지 피드백

---

## [있는데 연결 안 된 것] 마무리 필요

### 11. MaxHp StatType 정리
- StatType에 정의되어 있으나 스태미나=HP 방향이므로 사용처 없음
- MaxStamina와 역할 중복 → 제거하거나 주석으로 비활성화

### 12. 정착지(Settlement) → 다음날 루프 연결
- SettlementManager, SettlementUI 존재하지만 날짜 진행 흐름에 미연결
- 자발 복귀와 게임오버 복귀 두 경로 모두 Settlement 정산을 거쳐야 함

---

## [참고] 이미 잘 구현된 것 (보충 불필요)
- 채굴 + 스태미나 연동 (DamageZone → UseStamina)
- 자발적 지상 복귀 흐름 (ExploreExitController)
- 판매 / 업그레이드 / 창고
- 함정 시스템 (CollapseFloor, RollingRockTrap, StalactiteTrap 등)
- 조명 / 시야 (FieldOfView, FlashlightController)
- 피격 플래시 (HitFlashUI)
- 세이브 / 로드
- 다국어 지원
